# Installer state and recovery material must remain outside Git.
*.agekey
install-state.age
install-state.age.*
*.pem

# Environment secret files are expected to be SOPS-encrypted and committed.
# Do not add a broad secrets*.yaml ignore rule here: that can make an ArgoCD
# installation silently omit the generated credentials.
